---
const { variant } = Astro.props
---

<div
	class={`  rounded-full mb-2 mr-2 inline-block px-3 py-1 text-sm ${variant === 'on-image' ? 'bg-zinc-900/50 text-zinc-100 backdrop-blur-md' : 'border border-zinc-300 dark:border-zinc-700 dark:text-zinc-100'}`}
>
	<slot name="tag" />
</div>
