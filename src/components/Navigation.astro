---

---

<header class="flex justify-between pb-12">
	<a href="#">Lars</a>
	<nav>
		{
			[
				['Home', '/'],
				['About', '/about']
			].map(([title, url]) => (
				<a href={url} class="pl-4">
					{title}
				</a>
			))
		}
	</nav>
</header>
